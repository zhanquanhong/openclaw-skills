# AGENTS.md - 资深开发工作区

_你的工作区规范。每次开发前必读，严格遵守。_

## 开发流程（必须遵守）

### 1. 需求确认
- [ ] 与用户确认清楚需求
- [ ] 梳理核心需求、约束条件、交付物
- [ ] 理解业务意图（表面需求 → 业务目标）
- [ ] 输出：需求文档

### 2. 方案设计
- [ ] 基于需求设计完整方案
- [ ] 包括架构设计、模块划分、数据流转
- [ ] 考虑边界条件、异常场景、性能影响
- [ ] 给出多种方案 + 推荐
- [ ] 输出：设计方案文档

### 3. 用户确认
- [ ] 等待用户确认设计方案
- [ ] 用户说"可以"后再继续
- [ ] **不能跳过此步骤**

### 4. 编码实施
- [ ] 按设计方案执行
- [ ] 遵循生产级代码规范
- [ ] 类型注解、docstring、异常处理、日志

### 5. 自检验证
- [ ] 运行单元测试
- [ ] 验证数据完整性
- [ ] 清理临时文件
- [ ] 提交前代码审查

---

## 代码审查清单

每次提交前必须检查：

### 文件清理
- [ ] 无残留文件（临时/备份/多版本）
- [ ] 无 __pycache__/.venv/.pytest_cache
- [ ] 无 .git 目录（技能目录内）

### 代码质量
- [ ] 无重复代码（相同逻辑只在一处）
- [ ] 无 `pass` 占位（所有方法都有实现）
- [ ] 无 `TODO` 留给生产
- [ ] 无空壳实现

### 生产级标准
- [ ] 类型注解完整
- [ ] 文档字符串完整
- [ ] 异常处理合理
- [ ] 日志记录充分
- [ ] 单元测试覆盖

---

## 文件组织规范

```
workspace/
├── skills/           # OpenClaw 技能
│   ├── tech-doc-validator/
│   ├── wbs-skill/
│   └── ...
├── agents/           # 智能体
├── docs/             # 文档
├── scripts/          # 脚本工具
├── test-data/        # 测试数据
└── output/           # 输出目录
```

---

## Git 提交规范

### Commit Message 格式
```
feat: 新功能
fix: 修复 Bug
docs: 文档更新
style: 代码格式（不影响功能）
refactor: 重构
test: 测试相关
chore: 构建/工具/配置
```

### 发布检查清单
- [ ] 更新版本号（严格递增）
- [ ] 清理临时文件
- [ ] 运行测试
- [ ] 提交代码
- [ ] 创建 GitHub Release
- [ ] 上传 ZIP 文件

---

## 工具使用

### 本地测试
```bash
# Python 技能
cd skills/xxx
uv venv
source .venv/bin/activate
uv pip install -r requirements.txt
python test_xxx.py

# 验证 WBS 生成
python generate_wbs.py test-data/xxx.pdf
```

### 代码质量
```bash
# 检查重复代码
radon cc .

# 检查代码风格
flake8 .
black --check .
```

---

## 会话流程

### 每次会话前
1. 读取 CONTEXT.md（当前任务上下文）
2. 读取 MEMORY.md（相关记忆）
3. 读取 TOOLS.md（用户偏好）
4. 读取 PROMPTS.md（意图理解提示）
5. **读取 SKILL_REGISTRY.md**（技能清单和区别）

### 技能识别流程（新增）

**1. 扫描技能目录**
```bash
find skills/ -name "skill.json" -type f
```

**2. 读取 SKILL_REGISTRY.md**
- 了解当前所有技能
- 识别功能相似的技能
- 注意可能混淆的点

**3. 询问用户**（不确定时）
- "您说的是哪个技能？"
- "需要用 java-wbs-decomposition 还是 wbs-skill？"

### 技能输出规范

**1. 明确技能名称**
- 开场说明："我现在使用 [技能名称]..."
- 避免模糊："我用 WBS 工具" → "我用 java-wbs-decomposition"

**2. 说明选择理由**
- "因为您需要完整分工，所以用 java-wbs-decomposition"
- "因为您需要快速分解，所以用 wbs-skill"

**3. 输出前确认**
- "我准备用 [技能名称] 生成 [输出格式]，对吗？"

### 每次会话后（必须执行）

**1. 更新 CONTEXT.md**
- [ ] 添加对话记录到表格
- [ ] 更新待确认事项
- [ ] 更新关键决策

**2. 询问用户是否记录到 MEMORY.md**
- [ ] 如果有新经验/技术决策 → 问："这个要记录到 MEMORY.md 吗？"
- [ ] 如果有踩坑/错误 → 问："这个踩坑要记录到 MEMORY.md 吗？"
- [ ] 如果有新偏好 → 问："这个偏好要更新到 TOOLS.md 吗？"

**3. Git 提交前必须询问**
- [ ] **提交前必须问过用户**："这些文件要提交到 GitHub 吗？"
- [ ] 说明提交内容（文件清单 + 提交信息）
- [ ] 等用户确认后再执行 `git push`
- [ ] 配置文件（SOUL.md/USER.md/MEMORY.md 等）属于本地配置，不应擅自公开

**4. 清理临时文件**
- [ ] 删除 __pycache__/.venv/.pytest_cache
- [ ] 删除临时文件（*.tmp, *.bak）
- [ ] 删除多版本文件（只保留生产版本）

---

**违反后果**: 会被用户批评，必须立即补救并记录到 MEMORY.md

---

_严格遵守此规范，输出生产级代码。_

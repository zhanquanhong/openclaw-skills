# 图片压缩 API 服务依赖 (Python 3.6 兼容版)
# 安装：pip3 install --user -r requirements_py36.txt

# Web 框架 (Python 3.6 兼容版本)
fastapi==0.68.0
uvicorn==0.15.0
python-multipart==0.0.5

# 图像处理核心库
Pillow==8.4.0
pillow-heif==0.9.0

# HTTP 客户端 (用于 URL 压缩)
httpx==0.19.0

# 可选：性能优化
# pillow-simd  # SIMD 加速的 Pillow (可选，性能提升 30-50%)
